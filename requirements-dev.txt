# Locked development dependencies for alembic-converger
# These are the exact versions used during development
# Install with: pip install -r requirements-dev.txt

# Core dependencies
alembic>=1.7.0
click>=8.0
colorama>=0.4.0

# Development dependencies
pytest>=7.0
pytest-cov>=4.0
black>=23.0
ruff>=0.1.0

# Build and publish tools (optional)
build>=0.10
twine>=4.0
